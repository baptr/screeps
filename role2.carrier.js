const ROLE = 'carrier';



module.exports = {
ROLE: ROLE,
spawn: function(spawn) {
    // Make sure there's something to do.
    // And we can spawn a big enough one to be worth it.
    // Then do it.
},
run: function(creep) {
    
}
};